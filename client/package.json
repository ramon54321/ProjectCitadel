{
  "license": "MIT",
  "scripts": {
    "compile": "yarn run lint && webpack",
    "lint": "tslint -t stylish -p tsconfig.json",
    "clean": "rm -rf ./dist/*",
    "production:compile": "RUN_ENV=production yarn run lint && webpack",
    "devops:deploy": "./devops/scripts/deploy.sh"
  },
  "devDependencies": {
    "ts-loader": "^5.3.0",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.16.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.1.6",
    "webpack": "^4.26.0",
    "webpack-cli": "^3.1.2"
  }
}
